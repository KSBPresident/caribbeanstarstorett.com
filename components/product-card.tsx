type Product={name:string;category:string;price:string;location:string}; export function ProductCard({product}:{product:Product}){return <article className="product-card"><div className="product-art">{product.category.slice(0,1)}</div><div className="product-body"><span className="eyebrow">{product.category}</span><h3>{product.name}</h3><strong>{product.price}</strong><p>{product.location}</p><a href="/marketplace">View item</a></div></article>}