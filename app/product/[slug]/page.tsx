import {notFound} from "next/navigation";import {SiteHeader} from "../../../components/site-header";import {products,money} from "../../../lib/store-data";export default async function ProductDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const product=products.find(p=>p.slug===slug);if(!product)notFound();return <><SiteHeader/><main className="product-detail"><div className="breadcrumbs">Home › {product.category} › {product.name}</div><section className="detail-main"><div className="gallery"><div className="thumbs">{[1,2,3].map(i=><img key={i} src={product.image} alt=""/>)}</div><div className="main-product-image"><img src={product.image} alt={product.name}/></div></div><div className="detail-info"><h1>{product.name}</h1><div className="rating">★ <b>{product.rating}</b> ({product.reviews} reviews)</div><strong className="detail-price">{money(product.price)}</strong><p className="stock">● In Stock</p><div className="seller-box"><b>Sold by: {product.seller}</b><small>✓ Verified Store</small></div><ul className="delivery-list"><li>Free delivery (T&T)</li><li>7-10 day delivery</li><li>1-year warranty</li><li>30-day return policy</li></ul><div className="quantity"><button>−</button><span>1</span><button>+</button><button className="blue-btn">Add to Cart</button></div><button className="buy-now">Buy Now</button></div></section><section className="detail-tabs"><b>Description</b><span>Specifications</span><span>Reviews</span><div><p>{product.description}</p></div></section></main></>}